import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onthelisttechtz.merge2048',
  appName: '2048 Number Game: Merge Blocks',
  webDir: 'out'
};

export default config;
